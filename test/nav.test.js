var NavService = require('../src/components/nav/nav.service');
var chai = require('chai');
var chaiHttp = require('chai-http');
var expect = chai.expect;
chai.use(chaiHttp);

describe('test', function() {
    it('test for test', function(done) {
        expect("").to.equal("");
        done();
    });
});
